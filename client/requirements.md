## Packages
react-icons | For the WhatsApp icon and other brand icons
framer-motion | For scroll animations and premium visual effects

## Notes
The backend will handle Twilio integration for WhatsApp messaging on form submission.
The frontend uses standard HTTP POST to `/api/contact` using the exact schema from `@shared/routes`.
